interface:
  display_name: "Leanforge:Set"
  short_description: "Capture durable project context for trusted future changes."
  default_prompt: "Use /leanforge:set to capture durable project context for this project."

policy:
  allow_implicit_invocation: false
