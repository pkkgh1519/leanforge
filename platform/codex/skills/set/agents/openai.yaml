interface:
  display_name: "Leanforge:Set"
  short_description: "Capture durable project context for trusted future changes."
  default_prompt: "Capture durable project context for this project without rediscovering settled decisions."

policy:
  allow_implicit_invocation: false
